[Service]
# Restart when the process exits with non-zero, is terminated by a signal, or times out
Restart=on-failure
# Wait a bit before restarting to avoid tight crash loops
RestartSec=3

# Optional: add limits on how often restarts can happen before giving up
# (uncomment if needed)
# StartLimitIntervalSec=300
# StartLimitBurst=5
